local M = {}

local leveldata = require("Assets.levels.level-data")

-- local filename = system.pathForFile( "Assets/levels/leveldata.json", system.ResourceDirectory )
-- local decoded, pos, msg = json.decodeFile( filename )
 
-- if not decoded then
--     print( "Decode failed at "..tostring(pos)..": "..tostring(msg) )
-- else
--     print( "File successfully decoded!" )
-- end






return M